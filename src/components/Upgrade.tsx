// The paywall/upgrade screen has been removed for now. This placeholder is kept
// so any stale imports don't break the build; it renders nothing and is not
// wired into the app's navigation.
export default function Upgrade() {
  return null;
}
